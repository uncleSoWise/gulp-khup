
        <!--Header-->
        <table width="<%= parseInt(contentWidth) + (gutterWidth * 2) %>" cellpadding="0" cellspacing="0" border="0" bgcolor="#000000" class="table" align="center">
          <tr>
            <td width="<%= parseInt(contentWidth) + (gutterWidth * 2) %>" class="logo"><a href="<%= companyUrl %>"><img width="<%= parseInt(contentWidth) + (gutterWidth * 2) %>" class="img-logo" src="img/image.jpg" alt="<%= companyName %>" /></a></td>
          </tr>
        </table>
